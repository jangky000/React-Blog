import React, { FC } from 'react';
import {MainPage} from './pages/Main'

// routing

const App:FC = () => {
  return (
    <div style={{height:'100vh'}}>
      <MainPage />
    </div>
  );
}

export default App;
